# 项目问题记录与修复日志

## 2026-06-30 问题记录

### 1. 采集停止问题

**现象**: 
- `auto_collect_all.py` 运行到一半停止
- Gateway 环境无法显示采集输出
- 进程在后台运行但无反馈

**原因**:
- Gateway 环境限制，无法直接操作微信客户端
- `peekaboo` 工具需要 Mac 本地屏幕控制
- 采集脚本在 Gateway 运行不稳定

**解决方案**:
- 在 Mac 本地运行采集脚本
- 使用 `subprocess.run` 捕获输出
- 增加超时和重试机制

**状态**: ⚠️ 部分解决（需要本地运行）

---

### 2. 公众号名称解析错误

**现象**:
- CSV 文件中公众号显示为 "Unknown"
- 或显示为 "妆合规最新4"（包含数量后缀）

**原因**:
- `collect.py` 的 `parse_natural_language` 函数正则表达式错误
- 把 "最新4" 当作公众号名称的一部分

**修复**:
```python
# 修复前（错误）
pattern1 = r'(?:获取|搜索)\s*([^\s]+)\s*(?:关于|搜索)?\s*([^\d\s]*)\s*(?:的)?\s*(\d*)\s*篇'

# 修复后（正确）
pattern1 = r'(?:获取|搜索)\s*(.+?)(?:最新|的)?\s*(\d+)\s*篇'
# 清理公众号名称
account = re.sub(r'最新\d+.*$', '', account).strip()
```

**状态**: ✅ 已修复

---

### 3. 数据库 Unknown 记录

**现象**:
- 数据库中出现 "Unknown" 公众号
- 影响统计和查询

**修复**:
```sql
DELETE FROM articles WHERE wechat_name = 'Unknown';
```

**状态**: ✅ 已清理

---

### 4. 重复采集问题

**现象**:
- 同一篇文章被多次导入
- CSV 文件重复生成

**原因**:
- 没有检查文章是否已存在
- 多次运行采集脚本

**解决方案**:
- 导入前检查 URL 是否已存在
- 跳过已存在的文章

**状态**: ✅ 已修复

---

### 5. 公众号列表更新

**移除的公众号**（3个）:
- 美妆内行人
- 个护前言
- Beauty Insider

**原因**: 这些公众号没有文章或不需要采集

**当前列表**（12个）:
1. 妆研24小时
2. 非科学美妆传播
3. 原料合规观察
4. 妆合规
5. Fbeauty未来迹
6. 个护前沿
7. KEV美妆
8. 美业颜究院
9. 肤见未来实验室
10. 化妆品观察 品观
11. 中国化妆品
12. 上海日化协会

**状态**: ✅ 已更新

---

### 6. 时间范围解析错误

**现象**:
- 用户请求"最新5天"，系统返回"最新3天"的结果
- 默认3天覆盖了用户指定的天数

**原因**:
- 代码先检查固定关键词（"3天"、"7天"等）
- 最后才检查正则 `(\d+)\s*天`
- 匹配失败时使用默认的3天

**修复**:
```javascript
// 优先提取 "X天" 格式
const dayMatch = content.match(/(\d+)\s*天/);
if (dayMatch) {
  days = parseInt(dayMatch[1]);
}
// 然后检查固定关键词
else if (contentLower.includes('今天')) {
  days = 1;
}
// ...
```

**状态**: ✅ 已修复

---

## 修复记录

### 2026-06-30 修复

1. ✅ 修复 `collect.py` 解析逻辑
2. ✅ 清理数据库 Unknown 记录
3. ✅ 更新公众号列表（移除3个）
4. ✅ 添加导入去重检查
5. ✅ 更新项目文档

---

## 待办事项

- [ ] 优化采集脚本稳定性
- [ ] 添加采集进度显示
- [ ] 实现断点续采功能
- [ ] 添加采集失败重试机制

---

## 注意事项

1. **采集必须在 Mac 本地运行**
   - Gateway 环境无法操作微信
   - 需要 `peekaboo` 和微信客户端

2. **公众号名称不要包含数量**
   - 正确: "妆合规"
   - 错误: "妆合规最新4"

3. **定期清理 Unknown 记录**
   - 检查数据库中的异常记录
   - 保持数据清洁

---

> 记录时间: 2026-06-30 18:30
> 记录人: 搜搜 (Sōusou)
